grades:
    proposal:
        type: pass
    link:
        type: float
        subgrades:
            solution: integer
            design: integer
            process: integer
            report: integer
            code: integer
        calculation: ((solution + design + process + code + report) / 25.0 * 9 + 1).round(1)

calculation:
    normal:
        all: 1

all:
    required: true
    submits:
        github: 1
